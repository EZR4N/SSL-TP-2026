# Concatenate
Recibe un array de Strings y la longitud del Array. Devuelve el String concatenado. No admite un array vacío, o sea, de length = 0.

# Power
Recibe un String y cuantas veces se quiere repetir el mismo. Devuelve el String repetido. 

# Reverse
Recibe un String. Lo devuelve invertido.